pluginManagement {
    repositories {
        gradlePluginPortal()
        // ForgeGradle itself, and the Mixin Gradle plugin that generates the refmap.
        maven { name = 'MinecraftForge'; url = 'https://maven.minecraftforge.net/' }
        maven { name = 'Sponge'; url = 'https://repo.spongepowered.org/repository/maven-public/' }
    }
}

rootProject.name = 'stock-keeper-bookmarks'
